import React, {FC} from 'react';
import {IRecommendedData} from "@/types/recommended.interface";

export const RecommendedCards:FC <IRecommendedData> = ({salaryFrom, salaryTo, responsibility, requirement, id, name, area, areaID, alternate_url}) => {
  return (
    <div>

    </div>
  );
};

